import 'package:flutter/material.dart';
import 'package:topinfoads6/routes/routes_widget.dart';

void main() => runApp(
      const Banco(),
    );

class Banco extends StatelessWidget {
  const Banco({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // ignore: todo
    // TODO: implement build
    return RoutesWidget();
  }
}
